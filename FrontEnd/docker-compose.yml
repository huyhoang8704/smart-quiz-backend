version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app # Mount the host project directory to the container's /app
      - /app/node_modules # Ensure node_modules inside the container is not overwritten by host